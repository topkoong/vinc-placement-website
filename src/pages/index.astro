---
// Root index page that detects browser language and redirects
// This ensures users see the site in their preferred language automatically
---

<!doctype html>
<html lang='ja' class='dark'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>
      VINCプレースメント | タイ・日本間人材ソリューション | 企業・求職者向け
    </title>

    <!-- SEO Meta Tags -->
    <meta
      name='description'
      content='1988年創業、VINCプレースメントは日本の企業と熟練したタイ人労働者を結びます。10,000人以上のタイ人労働者を日本企業に派遣。日本の求人を探す、または建設・製造・エンジニアリングなど全業界でタイ人材を採用する。'
    />
    <meta
      name='keywords'
      content='タイ人労働者日本, 日本求人タイ, 日本企業タイ人労働者, タイ日本人材派遣, 日本ブルーカラー求人, タイ人ブルーカラー労働者日本, タイ日本労働力派遣, 建設労働者日本, 製造労働者日本, 工業労働者日本, エンジニア採用タイ日本, 国際人材ソリューション, 日本就職エージェンシー, タイ求職者日本'
    />

    <!-- Open Graph / Facebook Meta Tags -->
    <meta property='og:type' content='website' />
    <meta
      property='og:url'
      content='https://topkoong.github.io/vinc-placement-website/'
    />
    <meta
      property='og:title'
      content='VINCプレースメント | タイ・日本間人材ソリューション | 企業・求職者向け'
    />
    <meta
      property='og:description'
      content='1988年創業、VINCプレースメントは日本の企業と熟練したタイ人労働者を結びます。10,000人以上のタイ人労働者を日本企業に派遣。日本の求人を探す、または建設・製造・エンジニアリングなど全業界でタイ人材を採用する。'
    />
    <meta
      property='og:image'
      content='https://images.unsplash.com/photo-1581094794329-c8112a89af12?ixlib=rb-4.0.3&auto=format&fit=crop&w=1200&q=80'
    />
    <meta property='og:image:width' content='1200' />
    <meta property='og:image:height' content='630' />
    <meta property='og:locale' content='ja_JP' />

    <!-- Twitter Card Meta Tags -->
    <meta property='twitter:card' content='summary_large_image' />
    <meta
      property='twitter:url'
      content='https://topkoong.github.io/vinc-placement-website/'
    />
    <meta
      property='twitter:title'
      content='VINCプレースメント | タイ・日本間人材ソリューション | 企業・求職者向け'
    />
    <meta
      property='twitter:description'
      content='1988年創業、VINCプレースメントは日本の企業と熟練したタイ人労働者を結びます。10,000人以上のタイ人労働者を日本企業に派遣。日本の求人を探す、または建設・製造・エンジニアリングなど全業界でタイ人材を採用する。'
    />
    <meta
      property='twitter:image'
      content='https://images.unsplash.com/photo-1581094794329-c8112a89af12?ixlib=rb-4.0.3&auto=format&fit=crop&w=1200&q=80'
    />

    <!-- Canonical URL -->
    <link
      rel='canonical'
      href='https://topkoong.github.io/vinc-placement-website/'
    />

    <!-- Favicon -->
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <style>
      /* Loading screen styles */
      body {
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }
      .loader {
        text-align: center;
        color: white;
      }
      .spinner {
        border: 4px solid rgba(255, 255, 255, 0.3);
        border-top: 4px solid white;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        animation: spin 1s linear infinite;
        margin: 0 auto 20px;
      }
      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }
    </style>
  </head>
  <body>
    <!-- Hidden content for SEO and social media crawlers -->
    <div
      style='position: absolute; left: -10000px; top: auto; width: 1px; height: 1px; overflow: hidden;'
    >
      <h1>VINC Placement | Premier Thailand-Japan Workforce Solutions</h1>
      <p>
        Since 1988, VINC Placement connects Japanese employers with skilled Thai
        workers. Over 10,000 Thai workers placed in Japanese companies. Find
        jobs in Japan or hire Thai talent for construction, manufacturing,
        engineering, and all industries.
      </p>
      <p>
        Leading workforce solutions provider specializing in Thailand-Japan
        personnel deployment across all sectors.
      </p>
    </div>

    <div class='loader'>
      <div class='spinner'></div>
      <p>リダイレクト中... / Redirecting... / กำลังเปลี่ยนเส้นทาง...</p>
    </div>

    <script>
      // Language detection and redirect logic
      (function () {
        // Get browser language
        const browserLang = navigator.language || navigator.userLanguage;
        const langCode = browserLang.split('-')[0].toLowerCase();

        // Supported languages
        const supportedLangs = ['en', 'ja', 'th'];

        // Always default to Japanese unless specifically Thai
        // This ensures Japanese is the primary language for the site
        let targetLang = 'ja'; // Default to Japanese

        // Only redirect to Thai if browser is explicitly set to Thai
        if (langCode === 'th') {
          targetLang = 'th';
        }
        // Only redirect to English if browser is explicitly set to English AND user hasn't visited before
        else if (langCode === 'en') {
          // Check if user has a language preference stored
          const savedLang = localStorage.getItem('preferred-language');
          if (savedLang && supportedLangs.includes(savedLang)) {
            targetLang = savedLang;
          } else {
            // For first-time English users, still default to Japanese
            targetLang = 'ja';
          }
        }

        // Redirect to appropriate language version
        // Use relative path to maintain repository name in URL
        const currentPath = window.location.pathname;
        const basePath = currentPath.endsWith('/')
          ? currentPath.slice(0, -1)
          : currentPath;
        window.location.href = `${basePath}/${targetLang}/`;
      })();
    </script>
  </body>
</html>
